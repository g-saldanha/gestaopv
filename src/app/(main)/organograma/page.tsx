'use client';
import React, { Suspense } from 'react';

const Organograma = () => {
    return (
        <Suspense>
            <div className="grid">
                <div className="col-12">
                    <div className="card">
                        <h5>Organograma</h5>
                    </div>
                </div>
            </div>
        </Suspense>
    );
};

export default Organograma;
